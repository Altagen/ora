[package]
name = "ora"
version = "0.2.6"
edition = "2021"
authors = ["Ange Cesari"]
description = "Omni Repository for Archives - Decentralized package manager"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Altagen/Ora"
keywords = ["package-manager", "cli", "archives"]

[[bin]]
name = "ora"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive", "cargo", "env"] }
console = "0.16"
indicatif = "0.18"
dialoguer = "0.12"
tokio = { version = "1.49", features = ["full"] }
reqwest = { version = "0.12", features = ["rustls-tls", "json", "stream"] }
rustls = "0.23"
rustls-native-certs = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "1.0"
tar = "0.4"
flate2 = "1.1"
zip = "8"
xz2 = "0.1"
sha2 = "0.10"
sequoia-openpgp = { version = "2", optional = true }
git2 = { version = "0.20", features = ["vendored-openssl", "vendored-libgit2"] }
glob = "0.3"
urlencoding = "2.1"
tempfile = "3.26"
walkdir = "2.4"
dirs = "6.0"
anyhow = "1.0"
thiserror = "2.0"
log = "0.4"
env_logger = "0.11"
chrono = { version = "0.4", features = ["serde"] }
platforms = "3.8"
url = "2.5"
async-trait = "0.1"
regex = "1.12"
md5 = "0.8"
semver = "1.0"

[features]
default = []
gpg = ["sequoia-openpgp"]

[dev-dependencies]
mockito = "1.7"
assert_cmd = "2.2"
predicates = "3.1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[package.metadata.deb]
maintainer = "Altagen"
copyright = "2025, Altagen"
extended-description = "Secure, decentralized package manager for pre-compiled binaries."
section = "utility"
priority = "optional"
assets = [
    ["target/release/ora", "usr/bin/", "755"],
]

[package.metadata.generate-rpm]
assets = [
    { source = "target/release/ora", dest = "/usr/bin/ora", mode = "755" },
]
